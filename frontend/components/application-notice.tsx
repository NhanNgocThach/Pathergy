import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";

export function ApplicationNotice() {
  return <Alert><AlertTitle>Educational healthcare project</AlertTitle><AlertDescription>Pathergy does not provide medical advice. Use fictional information only.</AlertDescription></Alert>;
}
